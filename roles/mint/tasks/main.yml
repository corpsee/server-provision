---

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 20"
  set_fact:
    ansible_distribution_release: focal
  when:
    - ansible_distribution_release == "ulyana"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 20.1"
  set_fact:
    ansible_distribution_release: focal
  when:
    - ansible_distribution_release == "ulyssa"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 20.2"
  set_fact:
    ansible_distribution_release: focal
  when:
    - ansible_distribution_release == "uma"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 20.3"
  set_fact:
    ansible_distribution_release: focal
  when:
    - ansible_distribution_release == "una"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 21"
  set_fact:
    ansible_distribution_release: jammy
  when:
    - ansible_distribution_release == "vanessa"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 21.1"
  set_fact:
    ansible_distribution_release: jammy
  when:
    - ansible_distribution_release == "vera"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 21.2"
  set_fact:
    ansible_distribution_release: jammy
  when:
    - ansible_distribution_release == "victoria"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 21.3"
  set_fact:
    ansible_distribution_release: jammy
  when:
    - ansible_distribution_release == "virginia"

- name: "Fix 'ansible_distribution_release' fact for Linux Mint 22"
  set_fact:
    ansible_distribution_release: noble
  when:
    - ansible_distribution_release == "wilma"
