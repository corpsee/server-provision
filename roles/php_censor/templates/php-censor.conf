server {
    charset utf-8;
    client_max_body_size 128M;

    listen *:80;

    server_name {{ php_censor_hostname }} www.{{ php_censor_hostname }};
    root /var/www/{{ php_censor_hostname }}/public;

    #access_log /var/www/{{ php_censor_hostname }}/runtime/nginx_access.log;
    error_log  /var/www/{{ php_censor_hostname }}/runtime/nginx_errors.log warn;

    location ~* \.(htm|html|xhtml|jpg|jpeg|gif|png|css|zip|tar|tgz|gz|rar|bz2|doc|xls|exe|pdf|ppt|wav|bmp|rtf|swf|ico|flv|txt|docx|xlsx)$ {
        try_files $uri @fpm;
        expires   30d;
    }

    location / {
        try_files $uri @fpm;
    }

    location @fpm {
        fastcgi_pass unix:/run/php/php{{ php_censor_php_version }}-fpm.sock;

        include fastcgi_params;

        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
        fastcgi_param SCRIPT_NAME index.php;
    }
}
